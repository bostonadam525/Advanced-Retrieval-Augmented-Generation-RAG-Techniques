# Vector DBs in LangChain
* LangChain has API wrappers for all popular vector DBs.
* LangChain allows embedding model integration with vector DBs to store documents and embedding vectors.
* This allows a user to create, query, read, update and delete documents and embeddings 
  * "CRUD" or "CQRUD"
